# v0.5.4 regression replay

This replay validates the deterministic portions of the v0.5.3 feature set plus the v0.5.4 result-driven corrections without calling LM Studio.

## Frozen baseline replay

Saved v0.5.2 TC1–TC3 semantic sessions were revalidated with the v0.5.4 validator:

- TC1: REQ-001 = APPLICABILITY UNKNOWN / NO COMPLIANCE VERDICT; REQ-002 = APPLICABILITY UNKNOWN / NOT EVALUABLE; REQ-003 = APPLICABILITY UNKNOWN / NOT EVALUABLE; zero critical issues.
- TC2: REQ-101 = APPLICABLE / VIOLATED; REQ-102 = APPLICABLE / NOT EVALUABLE; REQ-103 = NOT APPLICABLE / NO COMPLIANCE VERDICT; zero critical issues.
- TC3: REQ-201 = APPLICABLE / VIOLATED with 550 ms vs 500 ms; REQ-202 = NOT APPLICABLE / NO COMPLIANCE VERDICT; zero critical issues.

## Overnight defects replayed from saved v0.5.2.1 sessions

- TEST-006: REQ-501 is now APPLICABLE / VIOLATED. A witnessed `ParkingCamera = ACTIVE` point while GearPosition D is established is preserved as sufficient nonconformance. REQ-502 remains NOT APPLICABLE.
- TEST-009: the existing semantically incomplete session now raises `HISTORICAL_SOURCE_UNACCOUNTED` instead of silently accepting an empty historical analysis.
- TEST-013: original verdicts remain VIOLATED / SATISFIED / NOT APPLICABLE with no critical issue.
- TEST-014: after-only BZD semantics remain conservative: REQ-1402 stays APPLICABILITY UNKNOWN / NOT EVALUABLE.
- TEST-015: REQ-1501 is promoted to APPLICABLE but remains NOT EVALUABLE because repeated samples do not prove the persistence interval. REQ-1502 remains APPLICABILITY UNKNOWN.
- TEST-016: REQ-1601 remains VIOLATED at 650 ms vs 300 ms and an explicit timing-source conflict is derived against the reported ~250 ms pass.
- TEST-018: each supplied historical precedent now produces `HISTORICAL_SOURCE_UNACCOUNTED` until the primary semantic repair explicitly accounts for it.
- TEST-019: parent/child inherited scope behavior remains unchanged; all three requirements remain NOT APPLICABLE.
- TEST-020: Variant A remains VIOLATED and B–F remain NOT APPLICABLE. `VariantA.FunctionRequest` is removed from the response-evaluation bucket; only VariantA status evidence remains.


## v0.5.4 result-driven replay from the first real-model v0.5.3 run

The later supplied TEST-005, TEST-011, TEST-012, and TEST-021 v0.5.3 sessions exposed additional integration gaps. Revalidating those saved semantic objects with the finalized deterministic layer now yields:

- TEST-005 / REQ-401 = APPLICABLE / NOT EVALUABLE, timing fact absent, zero critical validator errors; the incomplete event stream is represented as a response-event coverage need rather than routed to the fast model.
- TEST-011 / REQ-1001 = APPLICABILITY UNKNOWN / NOT EVALUABLE, timing fact absent, zero critical validator errors.
- TEST-021 / REQ-2105 = APPLICABLE / SATISFIED at the correlated 70.100 s point.
- TEST-021 / REQ-2108 = APPLICABLE / SATISFIED at the correlated 70.800 s point.
- A dedicated inherited-parent-scope guard preserves TEST-019-style child NOT APPLICABLE semantics.

## Assignment-only trace convention

The built-in TEST-003 and Overnight Bundle v1.1 now write state changes as ordinary assignments such as:

```text
10.000 s FunctionRequest = INACTIVE
10.100 s FunctionRequest = ACTIVE
```

The later row is internally annotated as a canonical transition because the previous timestamped value for the same signal/clock differs. Its raw evidence text remains `FunctionRequest = ACTIVE`. A first isolated ACTIVE sample remains a STATE_SAMPLE, protecting TEST-011 semantics, and repeated same-value samples remain STATE_SAMPLE, protecting TEST-015 semantics.

## Automated regression

`pytest -q`: **94 passed**.
