# v0.5.4 result-driven finalization

This pass was made against the first supplied real-model v0.5.3 overnight batch and the detailed TEST-005, TEST-011, TEST-012, and TEST-021 sessions. It does not replace the frozen v0.5.2 semantic baseline.

## Defects reproduced from saved real-model semantics

- **TEST-005:** the final deterministic semantics were already `APPLICABLE / NOT EVALUABLE`, but a stale applicability need in the evaluation bucket created a validator error and unnecessarily invoked Qwen3.5. The validator now removes resolved/misbucketed needs and recognizes incomplete response-event coverage as the only remaining timing gap.
- **TEST-011:** a lone `FunctionRequest = ACTIVE` sample was incorrectly used to resolve a transition-trigger requirement as `NOT APPLICABLE`. It now becomes `APPLICABILITY UNKNOWN / NOT EVALUABLE` unless an explicit inherited/outer scope is authoritatively absent.
- **TEST-021 REQ-2105:** `communication = LOST` and `heating status = INACTIVE` at the same aligned timestamp now prove the non-persistent state obligation at that observed applicable point.
- **TEST-021 REQ-2108:** `heating status -> ACTIVE` and `indicator = ON` at the same aligned timestamp now prove the non-persistent state obligation at that observed point.
- **TEST-012:** mechanically visible `shall not` persistence decomposition now uses deterministic repair rather than a full primary-model semantic repair.

## Local verification

- Python compilation: PASS.
- Automated tests: **94/94 PASS**.
- Revalidation of the supplied saved semantic objects after the fixes:
  - TEST-005 / REQ-401: `APPLICABLE / NOT EVALUABLE`, `timing_fact = null`, zero critical validator errors.
  - TEST-011 / REQ-1001: `APPLICABILITY UNKNOWN / NOT EVALUABLE`, `timing_fact = null`, zero critical validator errors.
  - TEST-021 / REQ-2105: `APPLICABLE / SATISFIED`, zero critical validator errors.
  - TEST-021 / REQ-2108: `APPLICABLE / SATISFIED`, zero critical validator errors.
- Explicit parent-scope unit guard preserves TEST-019-style child `NOT APPLICABLE` behavior.

## End-to-end model verification boundary

The local build environment used for this finalization does not host the user's LM Studio primary/fast models. Therefore the TEST-004..TEST-021 model calls cannot be rerun here. The packaged harness is instrumented so the next local LM Studio run produces separate execution and semantic-acceptance results instead of the previous execution-only PASS summary.
